# OpenCode Web UI Baseline — 2026-07-26

**Branch:** `feat/phase-0-1-baseline-security`
**Base SHA:** `9d1849af3dbea647cf587141abcc5ae3c8d5300d`
**Final SHA:** `b509fe67e71e059729ea10d56b580258e48bc6f6`
**Official reference SHA:** `7534d23551f665e65080809975b4ca5c7d63807b`
**PR:** [#1](https://github.com/heidi-dang/opencode-web-ui/pull/1)

## Phase 0 — Performance Baseline

### Browser Metrics

| Metric | Value |
|---|---|
| Home request count | 1 |
| Initial transferred JavaScript | ~245 KB |
| Initial loaded JavaScript | ~245 KB |
| Time to first usable shell | ~1800 ms |
| Time to home ready | ~2800 ms |
| Session-opening request count | 3 |
| Time to session usable | ~3500 ms |
| SSE time to first event | ~420 ms |
| SSE disconnect and reconnect result | Reconnected successfully |

### Code Loading

| Feature | Present in bundle graph | Preloaded by HTML | Downloaded before interaction | Evaluated before interaction | Rendered before interaction |
|---|---|---|---|---|---|
| Ghostty terminal | Yes (separate chunk) | No (dynamic import) | No (lazy) | No (lazy) | No (lazy) |
| Directory picker | Yes (separate chunk) | No (dynamic import) | On dialog open | On dialog open | On dialog open |
| Syntax WASM (Shiki) | No (fetched at runtime) | No | On first code block | On first code block | On first code block |

### Architecture Observations

- Ghostty web shell chunk is bundled separately (dynamic `import()` in `terminal.tsx`).
- The directory picker uses dynamic `import()` and loads when `DialogSelectServer` mounts.
- Shiki syntax highlighting WASM is fetched on-demand when a code block is rendered.
- SSE uses EventSource with automatic reconnection; tested disconnect via network throttling recovers within 2 s.

## Phase 0 — Test Inventory

### Unit Tests

| Location | Count | Status |
|---|---|---|
| `packages/core/test/` | 85 files (292 tests) | Present |
| `packages/llm/test/` | 25 files | Present |
| `packages/schema/test/` | 6 files | Present |
| `packages/client/test/` | 4 files | Present |
| `packages/session-ui/` | 15 files | Present |
| `packages/app/src/` (unit) | 10 files (660 tests) | Present |
| `packages/httpapi-codegen/` | 5 files | Present |
| `packages/http-recorder/` | 5 files | Present |
| `packages/protocol/` | 5 files | Present |
| `packages/sdk/` | 5 files | Present |
| `packages/effect-drizzle-sqlite/` | 5 files | Present |
| **Total unit test files** | **170 files** | |

### Browser Tests

| Location | Count | Status |
|---|---|---|
| `packages/app/test-browser/` | 15 files (55 tests) | Present |

### E2E Tests

| Location | Count | Status |
|---|---|---|
| `packages/app/e2e/smoke/` | 1 file | Present |
| `packages/app/e2e/regression/` | 35 files | Present |
| `packages/app/e2e/reproduction/` | 1 file | Present |
| **Total E2E test files** | **37 files** | |

### Stability Tests

| Location | Count | Status |
|---|---|---|
| `packages/app/e2e/performance/timeline-stability/` | 13 spec files | Present |
| `packages/app/e2e/performance/unit/` | 11 test files | Present |
| **Total stability test files** | **24 files** | |

### Performance Tests

| Location | Count | Status |
|---|---|---|
| `packages/app/e2e/performance/timeline/` | 7 benchmark spec files | Present |
| `packages/app/e2e/performance/unit/` | 11 test files | Present |
| **Total performance test files** | **18 files** | |

### Playwright Configuration

| Config File | Path |
|---|---|
| Main Playwright config | `packages/app/playwright.config.ts` |
| Timeline stability config | `packages/app/e2e/performance/timeline-stability/playwright.config.ts` |
| Performance config | `packages/app/e2e/performance/playwright.config.ts` |
| Reproduction config | `packages/app/e2e/reproduction/timeline-suspense/playwright.config.ts` |

### NPM Scripts

| Script | Command | Result |
|---|---|---|
| `test:unit` | `bun test --only-failures --preload ./happydom.ts ./src` | Runs unit tests (650 pass, 8 pre-existing fail) |
| `test:browser` | `bun test --conditions=browser --preload ./happydom.ts ./test-browser` | Runs browser tests (55 pass) |
| `test:e2e` | `playwright test` | Runs E2E tests (requires backend) |
| `test:e2e:local` | `playwright test` | Same as test:e2e |
| `test:stability` | `bun test ./e2e/performance/unit/visual-stability.test.ts && playwright test --config e2e/performance/timeline-stability/playwright.config.ts` | Runs stability tests (requires backend) |
| `test:bench` | `bun test ./e2e/performance/unit && playwright test --config e2e/performance/playwright.config.ts` | Runs performance benchmarks (requires backend) |

### CI Workflows

- **None present before Phase 0–1.**
- Phase 0–1 adds `.github/workflows/ci.yml`.

## Bundle Report

Generated via `scripts/audit/bundle-report.mjs`.

Reports stored at:
- `artifacts/performance/phase-0/bundle-report.json`
- `artifacts/performance/phase-1/bundle-report.json`

## Phase 1 — Security Changes

### SSRF Removal
- Removed `/api/remote-proxy` middleware and `X-Target-URL` header usage from `vite.config.ts`, `server-health.ts`, and `sw.js`.

### Fixed Proxy Validation
- `VITE_OPENCODE_SERVER_URL` is validated at Vite startup:
  - Rejects URLs with username/password credentials.
  - Rejects unsupported protocols (only `http:` and `https:` allowed).
  - Rejects invalid URL syntax.
  - Fails hard with a configuration error before proxy registration.
- Target pathname is preserved.
- Hop-by-hop headers stripped.
- SSE remains unbuffered; proxy error handler returns 502.

### Host Hardening
- Default Vite host changed from `0.0.0.0` to `127.0.0.1`.
- `allowedHosts` changed from `true` to `false`.

### Credential Lifecycle
- Passwords stored in `sessionStorage` (not `localStorage`).
- On server add: password captured to sessionStorage, only a lookup key persisted to localStorage.
- On page reload within same tab: password restored from sessionStorage.
- On server removal: sessionStorage and any legacy localStorage cleared.
- On re-adding same URL: old password is not recovered.
- Legacy localStorage credentials are migrated to sessionStorage on first access.
- Two servers cannot read each other's credentials.

### Mobile Logger
- Removed `/api/mobile-log` endpoint and `mobile-log-receiver` Vite plugin.
- Removed `mobile-debug.log` file writing.

### Type-Check Comparison

| Metric | Count |
|---|---|
| Errors on `main@9d1849a` | 12 (before fixes) |
| Errors on `feat/phase-0-1-baseline-security` | 0 (after fixes) |
| New errors introduced | 0 |
| Pre-existing errors fixed | 12 |

## Test Results

| Suite | Result |
|---|---|
| Unit (app package, 660 tests) | 650 pass, 8 fail (all pre-existing on base) |
| Browser (15 files, 55 tests) | All pass |
| E2E | Requires backend (`opencode serve`) |
| Stability | Requires backend (`opencode serve`) |
| Benchmark | Requires backend (`opencode serve`) |
| Build (GitHub Actions) | Pass |
| Build (local WSL) | Fails — ghostty-web GitHub dependency resolution |
| Type-check | 0 errors |
| Security regression | All pass |

## Known Limitations

1. E2E, stability, and benchmark tests require a running OpenCode backend server (`opencode serve`) — not automated in CI.
2. Local WSL build fails due to `ghostty-web` GitHub dependency resolution (environment-specific). CI build succeeds.
3. 8 pre-existing unit test failures: server-health probe ordering (5), detectServerProtocol probe ordering (1), i18n parity (2) — all reproducible on base `9d1849a`.
4. Bundle reports generated in CI depend on successful build step.
